# glibc 术语表

> 本文档汇总 57 篇分析文档中使用的核心术语，按分类排列，附中英对照和简要释义。

---

## 目录

1. [线程与同步](#1-线程与同步)
2. [内存分配](#2-内存分配)
3. [动态链接与 ELF](#3-动态链接与-elf)
4. [信号处理](#4-信号处理)
5. [I/O 子系统](#5-io-子系统)
6. [AArch64 硬件特性](#6-aarch64-硬件特性)
7. [内核接口](#7-内核接口)
8. [通用系统编程](#8-通用系统编程)

---

## 1. 线程与同步

| 术语 | 英文 | 释义 |
|------|------|------|
| NPTL | Native POSIX Threads Library | glibc 的 POSIX 线程实现，自 glibc 2.34 起合并入 libc.so |
| TCB | Thread Control Block | 线程控制块，即 `struct pthread`，每个线程一个 |
| TLS | Thread-Local Storage | 线程本地存储，通过 `__thread` 或 `thread_local` 声明的变量 |
| TSD | Thread-Specific Data | POSIX 线程特定数据，通过 `pthread_key_create`/`pthread_getspecific` 访问 |
| DTV | Dynamic Thread Vector | 动态线程向量，TLS 模块查找表，`dtv_t` 数组 |
| TLSDESC | TLS Descriptor | TLS 描述符，AArch64 使用的延迟求值 TLS 访问机制 |
| tpidr_el0 | Thread Pointer ID Register (EL0) | AArch64 线程指针寄存器，指向 TCB |
| futex | Fast Userspace Mutex | 快速用户态互斥，Linux 内核提供的同步原语 |
| 三态协议 | Three-state protocol | futex 互斥锁的 0(空闲)/1(已锁无等待)/2(已锁有等待) 协议 |
| PI 锁 | Priority Inheritance lock | 优先级继承锁，避免优先级反转的互斥锁类型 |
| 健壮锁 | Robust mutex | 持有者进程死亡时内核自动标记的互斥锁 |
| 条件变量 | Condition Variable | pthread_cond，支持等待/通知的同步原语 |
| 双组机制 | Two-group mechanism | glibc 条件变量的核心算法，将等待者分成两组交替使用 |
| 屏障 | Barrier | pthread_barrier，所有线程到齐后同时继续 |
| 轮次模型 | Round model | 屏障的实现方式，使用轮次计数器避免虚假唤醒 |
| CAS | Compare-And-Swap | 比较并交换，原子操作的基本原语 |
| cancelhandling | Cancel handling field | `struct pthread` 中管理取消状态的位域字段 |
| SIGCANCEL | Signal Cancel | glibc 保留的实时信号 (32)，用于线程取消通知 |
| SIGSETXID | Signal Set XID | glibc 保留的实时信号 (33)，用于全线程凭据同步 |
| cleanup 处理器 | Cleanup handler | 线程取消或 `pthread_exit` 时自动执行的清理回调 |
| cleanup 双链 | Dual cleanup chain | `struct pthread` 中的 `cleanup`（旧式）和 `cleanup_jmp_buf`（新式）两条独立链 |
| ForcedUnwind | Forced Unwinding | POSIX 取消使用的强制栈展开，通过 `_Unwind_ForcedUnwind` 触发 |
| 栈金丝雀 | Stack canary | 栈溢出检测值，从 AT_RANDOM 获取 |
| guard page | Guard page | 栈底的不可访问保护页，防止栈溢出越界 |
| rseq | Restartable Sequences | 可重启序列，Linux 5.3+ 提供的 per-CPU 快速路径原语 |

## 2. 内存分配

| 术语 | 英文 | 释义 |
|------|------|------|
| ptmalloc2 | POSIX Thread malloc version 2 | glibc 的内存分配器实现 |
| chunk | Memory chunk | 内存分配的基本单元，包含头部元数据 |
| Arena | Allocation arena | 分配区，每个 arena 有独立的 bin 集合和 mutex |
| main_arena | Main arena | 主分配区，使用 sbrk 扩展 |
| tcache | Thread cache | 线程缓存，per-thread 无锁快速分配/释放路径 |
| tcache_key | Tcache key | tcache 防双重释放的校验值 |
| smallbin | Small bin | 小块 bin，固定大小，FIFO 双向链表 |
| largebin | Large bin | 大块 bin，按大小范围分组，按大小排序 |
| unsorted bin | Unsorted bin | 未排序 bin，新释放的大块暂存区 |
| top chunk | Top chunk | 顶部 chunk，arena 的连续未分配区域 |
| sysmalloc | System malloc | 向操作系统申请内存（sbrk 或 mmap） |
| mmap_threshold | Mmap threshold | 超过此大小的请求直接使用 mmap，默认 128KB |
| MADV_DONTNEED | Madvise DONTNEED | 释放物理页但保留虚拟映射的内核建议 |
| heap_info | Heap information | secondary arena 的 heap 头，记录 mmap 区域信息 |
| PREV_INUSE | Previous in use | chunk 标志位，表示前一个 chunk 是否在使用中 |
| IS_MMAPPED | Is mmapped | chunk 标志位，表示该 chunk 是否由 mmap 分配 |
| NON_MAIN_ARENA | Non main arena | chunk 标志位，表示该 chunk 不属于 main_arena |

## 3. 动态链接与 ELF

| 术语 | 英文 | 释义 |
|------|------|------|
| ld.so | Dynamic linker/loader | 动态链接器/加载器，即 `ld-linux-aarch64.so.1` |
| ELF | Executable and Linkable Format | 可执行与可链接格式，Linux 标准二进制格式 |
| link_map | Link map | 每个加载的 ELF 对象的描述结构 |
| GOT | Global Offset Table | 全局偏移表，存放全局数据/函数运行时地址 |
| PLT | Procedure Linkage Table | 过程链接表，函数调用的间接跳转表 |
| 延迟绑定 | Lazy binding | 函数首次调用时才解析地址，通过 PLT stub → _dl_fixup |
| RELRO | Relocation Read-Only | 重定位完成后将 GOT 设为只读（安全加固） |
| GNU hash | GNU hash table | 基于 bloom filter 和桶链的 O(1) 符号查找表 |
| bloom filter | Bloom filter | 概率数据结构，快速排除不存在的符号 |
| 符号版本化 | Symbol versioning | 同一符号可有多个版本共存，保证 ABI 向后兼容 |
| IFUNC | Indirect Function | 间接函数，运行时根据 CPU 特性选择最优实现 |
| PT_LOAD | Program header LOAD | ELF 程序头类型，标记需要加载到内存的段 |
| DT_NEEDED | Dynamic tag NEEDED | 动态段标记，列出依赖的共享库 |
| 搜索作用域 | Search scope | 符号查找时遍历的 link_map 有序集合 |
| 命名空间 | Namespace | dlopen 的 dlmopen 命名空间隔离 |
| r_scope_elem | Scope element | 搜索作用域的数组元素 |
| vDSO | Virtual Dynamic Shared Object | 内核映射到进程地址空间的虚拟共享库 |
| auxv | Auxiliary vector | 辅助向量，内核在栈上传递给进程的信息 |
| AT_SYSINFO_EHDR | Auxiliary type: vDSO base | 辅助向量条目，vDSO ELF 头地址 |
| ld.so.cache | Linker cache file | 共享库路径缓存，ldconfig 生成，ld.so 二分搜索使用 |
| ldconfig | Linker cache configurator | 管理 ld.so.cache 的用户态工具 |
| soname | Shared Object Name | 共享库的逻辑名称（DT_SONAME），如 `libfoo.so.1` |
| _dl_cache_libcmp | Cache library compare | ld.so.cache 中的版本感知库名比较函数 |
| glibc-hwcaps | glibc hardware capabilities | 按 CPU 能力选择优化库版本的子目录机制 |
| slotinfo | Slot information | TLS 模块全局注册表，`dtv_slotinfo_list` 链表 |
| TLS 代际 | TLS generation | TLS 模块变更的全局版本计数器，用于延迟更新 |
| 静态 TLS | Static TLS | 线程创建时预分配的 TLS 空间，IE/LE 访问模型使用 |
| 动态 TLS | Dynamic TLS | dlopen 后延迟分配的 TLS 空间，GD/LD 访问模型使用 |
| DFS 拓扑排序 | DFS topological sort | 基于深度优先搜索的依赖排序算法，确定初始化/析构顺序 |
| dl_find_object | dl find object | ld.so 内部快速地址→对象查找 API，O(log n) 二分搜索 |
| STM | Software Transactional Memory | 软件事务内存，dl-find_object 使用的并发安全机制 |
| _GNU_SOURCE | GNU Source macro | 特性测试宏，启用所有 GNU 扩展功能 |
| __USE_GNU | Use GNU guard | 内部宏，由 _GNU_SOURCE 触发，守卫 227 个功能块 |

## 4. 信号处理

| 术语 | 英文 | 释义 |
|------|------|------|
| sigaction | Signal action | 注册信号处理函数的 POSIX API |
| SA_RESTORER | Signal action restorer | sigaction 标志，安装信号返回跳板函数 |
| SA_SIGINFO | Signal action siginfo | sigaction 标志，使用三参数信号处理函数 |
| SA_ONSTACK | Signal action on stack | 信号处理在 sigaltstack 设定的备用栈上执行 |
| sigaltstack | Signal alternate stack | 为信号处理设置备用栈 |
| 信号帧 | Signal frame | 内核在用户栈上构建的信号上下文帧 |
| rt_sigreturn | Real-time signal return | 从信号处理函数返回内核的系统调用 |
| __restore_rt | Restore real-time | glibc 安装的信号返回跳板函数 |
| sigset_t | Signal set type | 信号集位掩码类型 |
| tgkill | Thread group kill | 向指定线程组的指定线程发送信号 |
| ucontext_t | User context type | 用户上下文结构，包含寄存器状态和信号掩码 |
| 实时信号 | Real-time signal | 信号值 32-64，支持排队，glibc 保留 32-33 |

## 5. I/O 子系统

| 术语 | 英文 | 释义 |
|------|------|------|
| _IO_FILE | Internal IO FILE | glibc 内部的 FILE 结构实现 |
| _IO_FILE_plus | FILE plus vtable | FILE 结构 + vtable 指针的组合结构 |
| _IO_jump_t | IO jump table | FILE 操作的虚函数跳转表（vtable） |
| vtable | Virtual function table | 虚函数表，包含 overflow/underflow/read/write 等操作 |
| overflow | Buffer overflow handler | 写缓冲区满时的处理回调 |
| underflow | Buffer underflow handler | 读缓冲区空时的填充回调 |
| _IO_list_all | IO list all | 全局 FILE 流链表头指针 |
| 全缓冲 | Full buffering | 缓冲区满时才刷新（文件默认模式） |
| 行缓冲 | Line buffering | 遇到换行符时刷新（终端默认模式） |
| 无缓冲 | Unbuffered | 每次操作直接执行系统调用（stderr 默认） |
| _IO_buf_base/end | IO buffer base/end | 指向实际 I/O 缓冲区的起止指针 |
| flockfile | File lock file | FILE 级别的递归锁加锁 |
| vtable 安全检查 | Vtable security check | 验证 vtable 指针在合法范围内，防止 FILE 利用攻击 |

## 6. AArch64 硬件特性

| 术语 | 英文 | 释义 |
|------|------|------|
| SVE | Scalable Vector Extension | ARM 可缩放向量扩展，向量长度 128-2048 位可变 |
| SME | Scalable Matrix Extension | ARM 矩阵扩展，提供 ZA 矩阵寄存器 |
| MOPS | Memory Operations | ARMv8.8 内存操作指令，硬件加速 memcpy/memset/memmove |
| MTE | Memory Tagging Extension | 内存标签扩展，硬件辅助 use-after-free/overflow 检测 |
| BTI | Branch Target Identification | 分支目标标识，防止 JOP/COP 攻击 |
| PAC | Pointer Authentication Code | 指针认证码，通过密码学签名保护返回地址和指针 |
| GCS | Guarded Control Stack | 守卫调用栈，硬件影子栈保护返回地址 |
| HWCAP / HWCAP2 | Hardware Capabilities | 硬件能力位，内核通过 auxv 传递 CPU 特性信息 |
| LSE | Large System Extension | ARMv8.1 大系统扩展，提供原子操作指令（LDADD 等） |
| LDXR / STXR | Load/Store Exclusive Register | ARMv8.0 独占加载/存储，用于实现 CAS |
| Variant PCS | Variant Procedure Call Standard | 变体过程调用标准，SVE/SME 函数使用的非标准调用约定 |

## 7. 内核接口

| 术语 | 英文 | 释义 |
|------|------|------|
| clone | Clone system call | 创建线程的系统调用，比 fork 更灵活 |
| futex | Fast Userspace Mutex syscall | 用户态/内核态协作的同步系统调用 |
| set_robust_list | Set robust list | 注册健壮锁列表，线程死亡时内核自动标记 |
| sbrk | Set break | 调整数据段末尾（main_arena 扩展方式） |
| mmap | Memory map | 内存映射系统调用 |
| mprotect | Memory protect | 修改内存区域保护属性 |
| madvise | Memory advise | 向内核提供内存使用建议 |
| rt_sigaction | Real-time sigaction | 64 位信号版本的信号注册系统调用 |
| rt_sigprocmask | Real-time sigprocmask | 64 位信号版本的信号掩码系统调用 |
| execve | Execute | 执行新程序，内核映射 ELF + ld.so |
| prctl | Process control | 进程控制系统调用（设置 MTE 模式等） |

## 8. 通用系统编程

| 术语 | 英文 | 释义 |
|------|------|------|
| ABI | Application Binary Interface | 应用程序二进制接口 |
| API | Application Programming Interface | 应用程序编程接口 |
| POSIX | Portable Operating System Interface | 可移植操作系统接口标准 |
| sysdeps | System dependencies | glibc 的架构/OS 特定代码目录 |
| 覆盖优先级 | Override priority | sysdeps 目录搜索路径：最具体的优先 |
| freeres | Free resources | glibc 退出时释放所有动态内存（方便 valgrind 检测） |
| interposition | Symbol interposition | 符号覆盖，LD_PRELOAD 库可替换 glibc 函数 |
| glibc 2.34 合并 | Library merge in 2.34 | libpthread、libdl、librt 功能合并入 libc.so |

---

> 本术语表与 [交叉索引.md](交叉索引.md) 及 [overview/README.md](overview/README.md) 配合使用。
